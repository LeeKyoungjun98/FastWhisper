# Faster-Whisper STT Server 의존성 패키지
# 설치: pip install -r requirements.txt

# 핵심 패키지
faster-whisper>=1.0.0
torch>=2.0.0
numpy<2
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6

# WebSocket
websockets>=11.0

# HTTP 클라이언트 (테스트용)
requests>=2.28.0
httpx>=0.24.0

# 선택적: 오디오 처리
# librosa>=0.10.0
